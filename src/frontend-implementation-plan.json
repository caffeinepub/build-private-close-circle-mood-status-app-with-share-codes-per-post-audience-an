{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Ensure Closest Connections reflects backend interaction counts after qualifying actions",
  "requirements": [
    {
      "id": "REQ-41",
      "summary": "Keep Closest Connections UI in sync with backend-provided interaction totals after status posts and Silent Signals by ensuring relevant queries are invalidated/refetched.",
      "acceptanceCriteria": [
        "After a user posts a status to a specific person / whole circle / only safe people, the Closest Connections page shows a non-zero Interactions value for affected people after data refetch.",
        "After a user posts a Silent Signal to a Safe Person, the Closest Connections page shows a non-zero Interactions value for that Safe Person after data refetch.",
        "No new user-facing copy is added/changed beyond what is necessary to surface the real number (keep existing UI text in English)."
      ],
      "file_operations": [
        {
          "path": "frontend/src/hooks/useQueries.ts",
          "operation": "modify",
          "description": "Update the Silent Signal post mutation to invalidate/refetch the Closest Connections query (and keep existing invalidations intact) so interaction totals refresh after posting a Silent Signal."
        }
      ]
    }
  ]
}